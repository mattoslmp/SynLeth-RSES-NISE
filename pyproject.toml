[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rses-onco"
version = "0.9.0"
description = "Coverage-aware all-NISE cancer vulnerability, pharmacology and scripted publication asset framework"
readme = "README.md"
requires-python = ">=3.10"
authors = [{name = "Leandro de Mattos Pereira"}]
dependencies = [
  "numpy>=1.24",
  "pandas>=2.0",
  "scipy>=1.10",
  "matplotlib>=3.7",
  "networkx>=3.1",
  "requests>=2.31",
  "pyyaml>=6.0",
  "openpyxl>=3.1",
]

[project.scripts]
rses-onco = "rses_onco.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
